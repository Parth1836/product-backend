// interface for Product response to client
export interface Product {
  id: number;
  title: string;
  description: string;
  category: string;
  price: string;
  rating: number;
  thumbnail: string;
}
